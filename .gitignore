# --- General ---
.DS_Store
.AppleDouble
.LSOverride
*.log
*.tmp
*.bak
*.swp
*.swo
*~
.env
.env.local
.env.*.local

# --- IDEs & Editors ---
.idea/
.vscode/
*.iml
*.ipr
*.iws
.classpath
.project
.settings/
*.sublime-workspace
*.sublime-project

# --- Java (Server & SDK) ---
.gradle/
build/
target/
server/build/
out/
bin/
*.class
*.jar
*.war
*.ear
hs_err_pid*
replay_pid*

# --- Node.js (SDK) ---
node_modules/
sdks/node/proto/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
dist/
coverage/
.nyc_output/
.eslintcache
.npm/
*.tsbuildinfo

# --- Python (SDK - uv) ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/
.hypotheses/
# uv
.venv/
# uv.lock is typically checked in for applications but can be ignored for libraries.
# Since this is an SDK, we might want to ignore it, but for a monorepo build it's often useful.
# Keeping it commented out means it WILL be tracked (default behavior).

# --- Go (SDK & Server) ---
# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
# Server executables (legacy)
server/server
# CLI binary at root
/cvt
# Test coverage
*.coverprofile
coverage.out
coverage.html
# Dependency directories (if not using modules or if using vendor)
vendor/
# Go workspace file (kept in repo for IDE support)
# go.work
# go.work.sum

# --- Docker ---
.docker/

# --- Terraform (Future proofing) ---
.terraform/
*.tfstate
*.tfstate.backup

# Auto Claude data directory
.auto-claude/

# Local skill drafts
bd

.playwright-test/
.playwright-mcp/
