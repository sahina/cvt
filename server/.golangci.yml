# golangci-lint configuration
version: "2"

linters:
  disable:
    - depguard  # Not needed for this project
  exclusions:
    paths:
      - pb  # Generated protobuf code
  settings:
    staticcheck:
      checks:
        - "all"  # Enable all checks including ST* (stylecheck)

run:
  timeout: 5m
